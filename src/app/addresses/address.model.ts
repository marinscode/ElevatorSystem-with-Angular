export interface Address {
    id: string,
    regnum: String,
    city: string,
    address: String,
    floors: String,
    numberElevators: Number,
    gfloor: Boolean,
    basement: Boolean,
    isActive: Boolean
}