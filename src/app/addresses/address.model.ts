export interface AddressModel {
    id: string,
    regnum: String,
    city: string,
    address: String,
    floors: String,
    numberElevators: Number,
    gfloor: Boolean,
    basement: Boolean,
    isActive: Boolean
}